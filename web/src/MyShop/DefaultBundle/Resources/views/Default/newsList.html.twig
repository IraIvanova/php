{% extends "@MyShopDefault/layout.html.twig" %}

{% block body %}
    <h2>News list</h2>
    <ul>
    {% for news in newsList %}
        <li>
            {{ news.getDatePublished().format("d.m.Y H:i:s") }}
            <b>{{ news.getTitle() }} </b><br>
            {{ news.getShortDescription() }}
            <br />
            <a href="{{ path("my_shop.full_news", {id:product.getId()}) }}"> Full news</a>
            <a href="{{ path("myshop.news_delete", {id:product.getId()}) }}">Удалить</a>
            <a href="{{ path("myshop.news_edit", {id:product.getId()}) }}">Редактировать</a>
        </li>
    {% else %}
        No products!
    {% endfor %}
    </ul>

{% endblock %}